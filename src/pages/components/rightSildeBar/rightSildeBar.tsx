import Taro, { Component } from '@tarojs/taro'
// 引入 Swiper, SwiperItem 组件
import { View } from '@tarojs/components'

export default class rightSildeBar extends Component {
  render () {
    return (
      <View> rightSildeBar </View>
    )
  }
}